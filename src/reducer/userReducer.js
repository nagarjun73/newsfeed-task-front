

const userReducer = (state, action) => {
  switch (action) {
    case "REGISTER": {
      return { ...state }
    }

    default: {
      return { ...state }
    }
  }

}


export default userReducer